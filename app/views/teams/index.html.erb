<h2>All Teams</h2>
<%= link_to "Create Team", new_team_path %>
<%= link_to "Create Player", new_player_path %>
<%= link_to "Sign Free Agent", player_freeagents_path %>


  <div >
    <%= form_for @draft, {action: "create"} do |f| %>
      <fieldset>

          Number of ROunds <%= f.number_field :number_of_rounds %>

          <%= f.submit "Create" %>

      </fieldset>
    <% end %>
  </div>

<%= link_to "Draft Roster", drafts_path %>

<ul>
  <% @teams.each do |team| %>
  <li>
    <%= link_to "#{team.name}", team_path(team) %>
  </li>
  <% end %>
</ul>

